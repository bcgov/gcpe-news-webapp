@using Gov.News.Api.Models

@model IEnumerable<ResourceLink>

@if (Model.Any())
{
    <div class="resource-links">
        <h3>Newsletters</h3>
        <ul>
            @foreach (var item in Model)
            {
                <li><a href="@item.Uri">@item.Key</a></li>
            }
        </ul>
    </div>
}