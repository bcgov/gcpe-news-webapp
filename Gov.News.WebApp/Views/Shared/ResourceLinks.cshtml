@using Gov.News.Website.Models
@model BaseViewModel 
<div class="resource-links">
    <h3>Resources</h3>
    <ul>
        @{int i = 0; }
        @foreach (var link in Model.ResourceLinks)
        {
            <li><a id='Resource_@(i)' href="@link.Uri">@link.Key</a></li>
            i++;
        }
    </ul>
</div>